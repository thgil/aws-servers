header#banner.hero-unit
  .image-container
    .image.image-1
  .image-container
    .image.image-2
  .image-container
    .image.image-3
  .container-fluid
    .intro
      h1 <tiny>get</tiny>Server<byte>Bytes<byte>
      p.lead <strong>Quality Game Servers On Demand.</strong> Pay when you play, pause when you don't and change games when you want.

      button.btn.btn-signup.btn-lg(type="button" ng-click="main.collapse()" ng-class="{'disabled':!main.canCollapse}")
        | Sign up for the alpha
      .row
        .col-xs-12.col-sm-offset-2.col-sm-8
          div(uib-collapse="main.isCollapsed").mail
            .caret-up
            .well
              .extra(ng-show="main.thanks")
                p.lead Thanks for signing up!
                p We will send you an email when we launch and give you <b>free</b> alpha access!
              div(ng-show="main.loading")
                i.fa.fa-spinner.spinner
              div(ng-hide="main.thanks || main.loading")
                form(name="form" ng-submit="main.submit(form)" novalidate)
                  p Put your email below and we will send you free alpha access as soon as we can!
                  .form-group(ng-class='{ "has-success": form.email.$valid && main.submitted,\
                  "has-error": form.email.$invalid && main.submitted }')
                    .input-group
                      input(type="text" name="email" ng-model="main.email" placeholder="your@email.com" overwrite-email mongoose-error required).form-control
                      .input-group-btn
                        button(type="submit").btn.btn-default.btn-mail Go
                  .form-group.has-error
                    p.help-block(ng-show='form.email.$error.email && main.submitted')
                      | Doesn't look like a valid email.
                    p.help-block(ng-show='form.email.$error.required && main.submitted')
                      | What's your email address?
                    p.help-block(ng-show='form.email.$error.mongoose')
                      | {{ main.error }}
      .row
        .extra(ng-if="!main.thanks")
          p Get a free 24 hours of server time if you sign up now
section.hide-overflow
  .container
    .col-sm-8.col-sm-offset-2.col-xs-12
      .row.feature
        .col-md-7
          h2.feature-heading Easy to Launch
          p.lead.
            Push button receive server.
            Just choose your game, tweak a few simple settings and start playing! No lengthy setup process means you get to game sooner.
        .col-md-5
          img(src="/assets/images/launchpad.jpg").feature-image.img-responsive.center-block
      .row.feature
        .col-md-5
          img(src="/assets/images/tools.jpg").feature-image.img-responsive.center-block
        .col-md-7
          h2.feature-heading Easy to Manage
          p.lead.
            Our simple dashboard makes it easy to manage your server, from basic settings to complex, server-specific customizations.
      .row.feature
        .col-md-7
          h2.feature-heading Super Flexible
          p.lead.
            Change your server settings, player slots, storage size and even the game—all at the touch of a button. Play what you want when you want.
        .col-md-5
          img(src="/assets/images/scale.jpg").feature-image.img-responsive.center-block
      .row.feature
        .col-md-5
          img(src="/assets/images/compass.jpg").feature-image.img-responsive.center-block
        .col-md-7
          h2.feature-heading Play as You Go
          p.lead.
            Add credits and play by the hour. Pause your play and your pay whenever you want.
      .row.feature
        .col-md-7
          h2.feature-heading More to come
          p.lead.
            Sign up to stay tuned with our upcoming features!
        .col-md-5
          img(src="/assets/images/experiment.jpg").feature-image.img-responsive.center-block
  .angle-rev-right
footer
